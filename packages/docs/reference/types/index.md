# Types

Public TypeScript contracts remain documented with the feature that owns them. This index provides direct symbol lookup without duplicating those definitions.

<ReferenceIndex symbol-kind="type" show-filters />
