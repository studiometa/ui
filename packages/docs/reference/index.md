# Reference

Browse every supported `@studiometa/ui` API by kind, task, package, runtime surface, or lifecycle status.

The reference distinguishes ready-to-use components from lower-level primitives, class decorators, helpers and public types. Each concept has one canonical page and can appear in several discovery views without duplicating its documentation.

<ReferenceIndex mode="overview" />
