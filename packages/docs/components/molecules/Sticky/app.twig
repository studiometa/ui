<div class="z-above relative max-w-2xl mx-auto mt-20">
  {% embed '@ui/molecules/Sticky/Sticky.twig' %}
    {% block content %}
      <div class="p-4 rounded bg-green-500 text-white font-bold">
        Sticky #1
      </div>
    {% endblock %}
  {% endembed %}

  <div class="h-24"></div>

  {% embed '@ui/molecules/Sticky/Sticky.twig'
    with { attr: { data_option_hide_when_down: true } }
  %}
    {% block content %}
      <div class="p-4 rounded bg-blue-500 text-white font-bold">
        Sticky #2 hideWhenDown<br />
        Multi line
      </div>
    {% endblock %}
  {% endembed %}

  <div class="h-96"></div>

  <div class="relative h-screen rounded bg-white">
    {% embed '@ui/molecules/Sticky/Sticky.twig'
      with { attr: { data_option_hide_when_up: true } }
    %}
      {% block content %}
        <div class="p-4 rounded bg-purple-500 text-white font-bold">
          Sticky #3 hideWhenUp
        </div>
      {% endblock %}
    {% endembed %}
  </div>

  <div class="h-96"></div>

  <div class="relative h-screen rounded bg-white">
    {% embed '@ui/molecules/Sticky/Sticky.twig'
      with { attr: { data_option_hide_when_down: true } }
    %}
      {% block content %}
        <div class="p-4 rounded bg-purple-500 text-white font-bold">
          Sticky #4 hideWhenDown
        </div>
      {% endblock %}
    {% endembed %}

    {% embed '@ui/molecules/Sticky/Sticky.twig' %}
      {% block content %}
        <div class="p-4 rounded bg-blue-500 text-white font-bold">
          Sticky #5
        </div>
      {% endblock %}
    {% endembed %}
  </div>

  <div class="h-96"></div>

  {% embed '@ui/molecules/Sticky/Sticky.twig' %}
    {% block content %}
      <div class="p-4 rounded bg-green-500 text-white font-bold">
        Sticky #6
      </div>
    {% endblock %}
  {% endembed %}

  <div class="h-screen"></div>
  <div class="h-screen rounded bg-white"></div>
  <div class="h-screen"></div>
</div>

<div class="fixed inset-0 top-1/2 flex items-center justify-center text-center pointer-events-none opacity-70">
  Scroll down<br>↓
</div>
