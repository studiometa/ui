{#
/**
 * @file
 * Hero component with a slider.
 *
 * @param string $title
 * @param string $subtitle
 * @param string $cta
 * @param 'left'|'center'|'right' $content_position
 */
#}

{% extends '@ui/molecules/Hero/Hero.twig' %}
{# TODO #}
{% block content %}
  {% for item in items %}
    {% block surtitle %}
      <div class="{{ html_classes(surtitle_classes|default('text-m')) }}">
        {{ surtilte }}
      </div>
    {% endblock %}
    {% block title %}
      {% html_element title_tag with title_attributes %}
        {{ title }}
      {% end_html_element %}
    {% endblock %}
    {% block subtitle %}
      <div class="{{ html_classes(subtitle_classes|default('text-s')) }}">
        {{ subtitle }}
      </div>
    {% endblock %}
    {% block cta %}
      {% include '@ui/atoms/Button/Button.twig' with { label: label } %}
    {% endblock %}
  {% endfor %}
{% endblock %}
