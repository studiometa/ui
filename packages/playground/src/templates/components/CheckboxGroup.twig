{#
/**
 * @file
 *   Radio.
 *
 * @param Array $items
 */
#}

<div class="relative flex items-center gap-4">
	{% for item in items %}
		{% include '@components/Checkbox.twig' with item only %}
	{% endfor %}
</div>
