// Full @studiometa/ui public barrel served as the manually-importable `/ui@<version>/index.js`
// CDN entry point. It re-exports every public export of @studiometa/ui so consumers can pull the
// whole library from one module while sharing the single externalized js-toolkit instance.
export * from '@studiometa/ui';
